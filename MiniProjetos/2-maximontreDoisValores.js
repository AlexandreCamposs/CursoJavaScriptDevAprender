// Escreva uma função que usa 2 números e retorna o maior entre eles.
let numeros = maior(5, 8);
console.log(numeros);

function maior(number1, number2) {
//   if (number1 > number2) return number1;
//   else return number2;

 return  number1>number2 ? number1: number2;
}
